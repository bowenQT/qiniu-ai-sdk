# Phase 3 Batch 1

Status: closed

Main baseline: `2cb6bba`

## Objective

Phase 3 Batch 1 focuses on promotion-backed productization:

- establish a dedicated nightly evidence source for promotion-sensitive modules
- promote only the minimum live-backed `ResponseAPI` subset
- reduce one held risk from `NodeMCPHost` without expanding MCP runtime surface
- align repo-facing narrative with the promoted subset and the remaining held basis

## Package Queue

1. `phase3/dx-validation/nightly-promotion-matrix`
2. `phase3/cloud-surface/responseapi-beta-promotion`
3. `phase3/node-integrations/mcphost-held-risk-reduction`
4. `phase3/foundation/product-surface-docs-sync`

## Worktree Order

### Stage A

- start only `nightly-promotion-matrix`
- freeze nightly artifact paths and profile wiring before opening promotion packages

### Stage B

- run `responseapi-beta-promotion` and `mcphost-held-risk-reduction` in parallel
- both packages must consume the nightly evidence contract from Stage A

### Stage C

- run `product-surface-docs-sync` after packages 2 and 3 land

## Integration Rules

- Package 1 merges first
- Packages 2 and 3 merge in either order, whichever conflicts less
- Package 4 merges last
- no package may expand `ResponseAPI` or `NodeMCPHost` public surface beyond the approved subset

## Acceptance Targets

- nightly promotion workflow exists and produces stable nightly artifacts
- `ResponseAPI` beta promotion is limited to `create`, `followUp`, `createTextResult`, and `followUpTextResult`
- `NodeMCPHost` remains `beta (held)` but removes the notifications/list_changed deferred risk
- README / COOKBOOK / doctor / scorecard / verification report all describe promoted subset versus deferred surface consistently

## Outcome

- Batch 1 landed on `main` at `2cb6bba`.
- `ResponseAPI` is now `beta`, but only for the core subset tracked in the Phase 3 decision artifact.
- `NodeMCPHost` remains `beta (held)` with the held basis narrowed to OAuth token acquisition and multi-server routing.
- A dedicated nightly promotion workflow now exists, but latest nightly evidence is not yet compiled into the tracked capability snapshot.
