# {{PROJECT_NAME}}

Agent quickstart for the Qiniu AI SDK.

## Run

```bash
npm install
cp .env.example .env
npm run dev
```

This starter uses `@bowenqt/qiniu-ai-sdk/qiniu` for the client and `/core` for runtime orchestration.
