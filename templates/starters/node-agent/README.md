# {{PROJECT_NAME}}

Node agent quickstart for the Qiniu AI SDK.

## Run

```bash
npm install
cp .env.example .env
npm run dev
```

This starter uses `/node` explicitly via `createNodeQiniuAI()`. Add `NodeMCPHost`, skills, or sandbox once the basic loop is working.
